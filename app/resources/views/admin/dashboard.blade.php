@extends('layouts.admin',['title'=>'نظرة عامة']) @section('content')
<div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">@foreach([['طلبات جديدة',$stats['newLeads'],'fa-inbox','emerald'],['كل الطلبات',$stats['leads'],'fa-briefcase','blue'],['مشتركون موثقون',$stats['subscribers'],'fa-user-check','violet'],['عمليات تحميل',$stats['downloads'],'fa-download','amber'],['ملفات الوسائط',$stats['media'],'fa-photo-film','rose']] as [$label,$value,$icon,$color])<article class="admin-card"><div class="flex items-center justify-between"><span class="grid h-11 w-11 place-items-center rounded-xl bg-slate-100 text-lg"><i class="fa-solid {{ $icon }}"></i></span><strong class="text-3xl font-black">{{ $value }}</strong></div><p class="mt-4 text-sm font-bold text-slate-500">{{ $label }}</p></article>@endforeach</div>
<div class="mt-6 grid gap-6 xl:grid-cols-3"><section class="admin-card xl:col-span-2"><div class="mb-4 flex items-center justify-between"><h2 class="text-lg font-extrabold">أحدث الطلبات</h2><a class="text-sm font-bold text-emerald-700" href="{{ route('admin.leads.index') }}">عرض الكل</a></div><div class="overflow-x-auto"><table class="admin-table"><thead><tr><th>العميل</th><th>الخدمة</th><th>الحالة</th><th>التاريخ</th></tr></thead><tbody>@forelse($recentLeads as $lead)<tr><td><a class="font-bold text-emerald-700" href="{{ route('admin.leads.show',$lead) }}">{{ $lead->name }}</a><small class="block text-slate-400">{{ $lead->email }}</small></td><td>{{ $lead->service }}</td><td><span class="rounded-full bg-slate-100 px-2 py-1 text-xs">{{ $lead->status }}</span></td><td>{{ $lead->created_at->diffForHumans() }}</td></tr>@empty<tr><td colspan="4" class="text-center text-slate-400">لا توجد طلبات بعد.</td></tr>@endforelse</tbody></table></div></section><section class="admin-card"><h2 class="mb-4 text-lg font-extrabold">نشاط الإدارة</h2><div class="space-y-4">@forelse($activities as $activity)<div class="border-r-2 border-emerald-400 pr-3"><strong class="block text-sm">{{ $activity->action }}</strong><small class="text-slate-400">{{ $activity->created_at->diffForHumans() }}</small></div>@empty<p class="text-slate-400">لا يوجد نشاط مسجل.</p>@endforelse</div></section></div>
@if(auth()->user()->must_change_password)<section class="admin-card mt-6 border-amber-300 bg-amber-50"><h2 class="font-extrabold text-amber-900">غيّر كلمة المرور الافتراضية</h2><form method="post" action="{{ route('admin.password.change') }}" class="mt-4 grid gap-3 md:grid-cols-4">@csrf @method('PUT')<input class="admin-input" name="current_password" type="password" placeholder="الحالية" required><input class="admin-input" name="password" type="password" placeholder="الجديدة" required><input class="admin-input" name="password_confirmation" type="password" placeholder="تأكيد الجديدة" required><button class="admin-btn">تحديث</button></form></section>@endif
@endsection
