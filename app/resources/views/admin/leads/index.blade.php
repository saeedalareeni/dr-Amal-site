@extends('layouts.admin',['title'=>'طلبات المشاريع']) @section('content')
<div class="mb-5 flex flex-wrap items-center justify-between gap-3"><form class="flex flex-wrap gap-2"><input class="admin-input" name="q" value="{{ request('q') }}" placeholder="اسم أو بريد"><select class="admin-input" name="status"><option value="">كل الحالات</option>@foreach(\App\Models\Lead::STATUSES as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select><button class="admin-btn-secondary">تصفية</button></form><a class="admin-btn" href="{{ route('admin.leads.export') }}"><i class="fa-solid fa-file-csv"></i>تصدير CSV</a></div>
<section class="admin-card overflow-x-auto p-0"><table class="admin-table"><thead><tr><th>#</th><th>العميل</th><th>الخدمة</th><th>الموعد</th><th>الحالة</th><th>الوصول</th></tr></thead><tbody>@forelse($leads as $lead)<tr><td>{{ $lead->id }}</td><td><strong>{{ $lead->name }}</strong><small class="block text-slate-400">{{ $lead->email }}</small></td><td>{{ $lead->service }}</td><td>{{ $lead->consultation_date?->format('Y-m-d') ?: '—' }}</td><td><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold">{{ $lead->status }}</span></td><td><a class="font-bold text-emerald-700" href="{{ route('admin.leads.show',$lead) }}">التفاصيل</a></td></tr>@empty<tr><td colspan="6" class="text-center text-slate-400">لا توجد نتائج.</td></tr>@endforelse</tbody></table></section><div class="mt-6">{{ $leads->links() }}</div>
@endsection
