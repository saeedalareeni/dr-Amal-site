@extends('layouts.admin',['title'=>'سجل النشاط']) @section('content')<section class="admin-card overflow-x-auto p-0"><table class="admin-table"><thead><tr><th>العملية</th><th>المستخدم</th><th>العنصر</th><th>IP</th><th>التاريخ</th></tr></thead><tbody>@forelse($activities as $activity)<tr><td class="font-bold">{{ $activity->action }}</td><td>{{ $activity->user?->name ?: 'النظام' }}</td><td>{{ class_basename($activity->subject_type ?: '') }} {{ $activity->subject_id }}</td><td dir="ltr">{{ $activity->ip_address }}</td><td>{{ $activity->created_at->format('Y-m-d H:i') }}</td></tr>@empty<tr><td colspan="5" class="text-center text-slate-400">لا يوجد نشاط.</td></tr>@endforelse</tbody></table></section><div class="mt-6">{{ $activities->links() }}</div>@endsection
