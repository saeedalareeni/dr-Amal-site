<!doctype html><html lang="{{ $subscriber->locale }}" dir="{{ $subscriber->locale === 'ar' ? 'rtl' : 'ltr' }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $subscriber->locale === 'en' ? 'Your resources' : 'ملفاتك المجانية' }}</title>@vite('resources/css/app.css')</head>
<body class="min-h-screen bg-slate-950 p-6 text-slate-100"><main class="mx-auto max-w-3xl py-16"><div class="rounded-3xl border border-white/10 bg-white/5 p-8"><h1 class="text-3xl font-bold">{{ $subscriber->locale === 'en' ? 'Your free resources are ready' : 'ملفاتك المجانية جاهزة' }}</h1><p class="mt-3 text-slate-300">{{ $subscriber->email }}</p><div class="mt-8 grid gap-4">@foreach($resources as $resource)@if($resource['visible'])<a class="rounded-2xl bg-emerald-500 px-5 py-4 font-bold text-slate-950" href="{{ URL::temporarySignedRoute('downloads.file', now()->addMinutes(30), ['subscriber' => $subscriber->id, 'resource' => $resource['key']]) }}">{{ $resource['title'][$subscriber->locale] ?? $resource['title']['ar'] }}</a>@endif @endforeach</div></div></main></body></html>
